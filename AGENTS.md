# Agent Rules & Guidelines (AGENTS.md)

본 문서는 의정부 경전철 월간 HTML 분석 보고서 자동 생성 Skill 개발에 있어 에이전트가 준수해야 할 개발 원칙과 가이드라인을 정의합니다.

## 1. 에이전트 작업 원칙
- **Surgical Precision**: 불필요한 리팩토링이나 범위를 벗어난 과도한 수정을 금합니다. 수정이 필요한 파일의 해당 라인만 정밀하게 작업합니다.
- **Read-Before-Write**: 파일을 수정하기 전에 대상 파일 및 인접 의존성 구조를 반드시 읽고 분석합니다.
- **Evidence-Based Verification**: 코드를 변경한 경우 반드시 CLI 명령을 통해 실행 가능성과 산출물 생성 여부를 직접 검증합니다.

## 2. 데이터 제약 사항
- **Raw 데이터 수정 금지**: `data/raw/` 경로 아래의 원본 데이터(승하차 CSV, 다운로드한 기상 JSON 등)는 절대 수정하지 않으며 읽기 전용으로만 다룹니다.
- **API Key 차단**: API Key 등록이나 과금이 요구되는 서비스는 사용하지 않습니다. 날씨 수집 등은 오픈 인증 기반의 Open-Meteo Archive API를 활용합니다.

## 3. 기능 개발 우선순위
- **월간 보고서 우선 완결**: 계절별, 반기별, 연도별 누적 분석 등 복잡한 비즈니스 로직은 차기 단계로 보류하고, 단일 월(YYYY-MM) 분석 및 HTML 렌더링에만 집중합니다.

## 4. 모듈별 역할 정의 및 관심사 분리
- **Python**: 복잡한 수치 계산, 검증(Validation), Matplotlib 차트 생성 및 HTML 데이터 매핑(Jinja2 렌더링)을 전담합니다.
- **LLM / Narrative**: 가공 완료된 `report_data.json` 및 `external_context.json`의 요약 값만을 전달받아 보고서 서술 문장을 생성합니다. 원본 데이터를 직접 가공하게 해서는 안 됩니다.
- **Collector**: 외부 데이터를 스크래핑/API 호출하여 가공 없이 캐시하는 역할만 가집니다.
- **Utility (scripts/utils/)**: 사람이 읽기 쉬운 형태의 값 변환(기상 코드의 한글 변환, 8방위 풍향 가독화, 요일 한글화 등)을 전담합니다.
- **HTML Renderer**: 템플릿 코드 내부에서 직접 Utility 모듈을 임포트하거나 호출하지 않습니다. 필요한 텍스트 및 테이블 행 데이터는 사전에 Analyzer 혹은 Context 가공 단계에서 완성된 텍스트로 `report_data.json`에 담겨 제공되어야 합니다.

## 5. 디자인 기준 및 시각화
- `templates/design.md`에 정의된 색상 스키마(Hex 코드), 여백 가이드, 폰트 규칙을 엄격히 분석하여 차트 시각화 및 HTML 스타일링에 일관되게 반영해야 합니다.

## 6. 예외 처리 및 Fallback 정책
- 외부 네트워크 지연 또는 홈페이지 차단 등으로 인해 크롤링/날씨 API 호출이 실패할 경우, 로컬에 이미 캐시된 데이터 혹은 사전 준비된 디폴트 CSV/JSON 백업본(Fallback)을 읽어 들이는 방어적 로직을 구현합니다.

## 7. 사용자 대화 및 프롬프팅 지침
- **데이터 누락 안내**: 작업 지시 시 `data/raw/ridership` 폴더가 비어있거나 분석 대상 월의 데이터가 없는 경우, 사용자에게 명시적으로 "https://www.data.go.kr/ 에 접속하여 '경기도 의정부시_경전철 승하차 현황'을 검색 후 해당 월 CSV 데이터를 받아 `data/raw/ridership` 폴더에 넣어주세요."라고 요청(응답)해야 합니다.
- **다중 데이터 시 선택 요구**: "보고서 작성해줘"와 같은 요청 시 `data/raw/ridership` 폴더 내에 2개 이상의 월별 데이터 파일이 존재한다면, 임의로 실행하지 말고 "어느 월(YYYY-MM)을 희망하시나요?"라고 물어본 뒤 선택을 받아 진행해야 합니다.
- **출력물 포맷 및 PDF 변환**: 보고서 생성의 기본 산출물은 HTML입니다. 작업 완료 시 "HTML 보고서 작성이 완료되었습니다. PDF로도 만들어 드릴까요?"라고 묻거나, 사용자가 "PDF도 만들어줘"라고 별도 요청을 했을 때에만 PDF 변환 작업을 수행하고 완료 여부를 간략히 응답합니다.
- **진행 상태 알림 및 경로 제공**: 사용자가 월을 선택하여 분석 스크립트 실행이 결정되면 "보고서 작성을 시작합니다."라고 간략히 알립니다. 최종 렌더링(또는 PDF 변환)까지 모두 끝난 후에는 "보고서 작성이 완료되었습니다."라는 안내와 함께, 결과물이 저장된 폴더의 경로를 제공해야 합니다. 단, 클릭 시 IDE 내부에서 에러 팝업이나 편집기가 뜨는 마크다운 하이퍼링크(`[text](url)`) 형태는 절대 사용하지 않으며, 대화창에 지저분하게 드러나지 않도록 `outputs/monthly/2026-05`와 같이 백틱(`)으로 감싼 단순 상대 경로 텍스트로만 표기하여 가독성을 높여야 합니다.

## 8. 작업 전 모델 적합성 점검 (사전 게이트)
- 모든 구현·수정 요청을 시작하기 전에 요구사항의 위험도, 영향 범위와 추론 난이도를 분석합니다.
- 분석 결과에 따라 구현 모델과 검증 모델의 권장 수준을 먼저 판단합니다.
- 모델 적합성 판단을 위해 필요한 최소한의 읽기 전용 조사는 수행할 수 있지만, 적합성 판단 전에는 파일을 수정하지 않습니다.
- 다음 고위험 작업은 비용 효율적인 하위 모델만으로 설계와 구현을 완료하지 않습니다:
  - 아키텍처 또는 데이터 파이프라인 흐름 변경 (`Collector` -> `Analyzer` -> `Jinja2 Renderer` 계약 등)
  - 여러 모듈·스크립트에 걸친 변경 (`scripts/`, `templates/`, `config/` 등)
  - 데이터 스키마(`report_data.json`), 차트 시각화 엔진, Jinja2 템플릿 계약 변경
  - 보안, 외부 API 연동 정책, 데이터 무결성 관련 작업
  - 원인과 해결책의 불확실성이 높은 버그
- 고위험 작업은 상위 추론 모델의 사전 분석 없이 실질적인 수정을 시작하지 않습니다.
- 실질적인 코드·설정·데이터·산출물 변경은 작업 후 상위 검증 모델로 확인합니다.
- 상위 모델을 사용할 수 없는 경우에는 자동 전환을 가장하지 않고, 사용자에게 제한 사항과 대체 검증 방법을 보고합니다.
- 저위험 작업은 비용 효율적인 구현 모델이 수행할 수 있습니다.
- 저위험 작업으로 판정된 경우 모델 적합성에 대한 별도 보고는 생략할 수 있지만, 기존의 작업 계획 보고와 사용자 승인 절차는 반드시 따릅니다.
- 현재 선택된 모델이 요구사항에 비해 낮다고 판단되면 파일 수정 전에 아래 형식으로 먼저 보고하고 지시를 대기합니다:
  ```text
  [모델 적합성 점검]
  - 작업 위험도: 높음 / 중간 / 낮음
  - 현재 모델: (현재 선택된 모델 또는 모델 등급)
  - 권장 구현 모델 수준: 비용 효율형 / 일반형 / 상위 추론형
  - 권장 검증 모델 수준: 일반 검증형 / 상위 검증형
  - 권장 이유: (예: 다중 모듈 수정 및 데이터 파이프라인 계약 영향 큼)
  - 낮은 모델로 진행할 때 위험: (예: 데이터 파싱 오류 및 렌더링 실패 가능성)
  - 권장 선택지:
    1) 상위 모델로 전환하여 분석·구현·검증 진행 (권장)
    2) 현재 모델로 읽기 전용 사전 조사 또는 PoC 분석만 수행
    3) 현재 모델로 진행하되, 실질적인 수정 전 사용자 승인 획득
  ```
- 사전 조사·PoC 결과는 참고 자료로만 취급하며, 상위 모델 검토나 사용자 승인 없이 최종 구현·배포에 사용하지 않습니다.

## 9. 위험도 기반 모델 라우팅 및 검증
- **모델 중립 원칙**: 기본 구현 모델은 프로젝트와 실행 환경에 따라 선택하며 특정 모델에 고정하지 않습니다.
- **저위험 작업**: 읽기 전용 조사, 설명, 단순 오탈자·주석 수정, 동작에 영향을 주지 않는 문서 표현 수정과 포맷 조정은 비용 효율적인 구현 모델이 처리합니다.
- **고위험 작업 및 분석**: 아키텍처, 데이터 파이프라인, 공개 계약, 보안, 난제 버그는 수정 전에 상위 추론 모델의 분석을 요청합니다.
- **실질적 변경의 사후 검증**: 분석 로직, 렌더링 규칙, 데이터 형식, 산출물 결과에 영향을 주는 변경은 작업 후 상위 검증 모델로 확인합니다.
- **검증 근거**: 상위 검증 모델은 실제 diff, 스크립트 실행 로그(`python scripts/generate_report.py` 등), 산출물 생성 여부를 기준으로 판단합니다.
- **권한과 역할**: 상위 분석·검증 모델은 기본적으로 읽기 전용 자문 역할을 수행하며, 파일 수정과 명령 실행은 승인 게이트와 사용자 지시를 따릅니다.
